# Required libraries for ScanCraft

PyQt6~=6.4             # PyQt5 for GUI application
PyTwain==2.3.0             # TWAIN support for scanner interaction
pytesseract==0.3.13        # Tesseract OCR Python wrapper
opencv-python==4.10.0.84   # OpenCV for image processing
Pillow==11.0.0             # Pillow for image processing
numpy==2.1.3               # NumPy, a dependency for OpenCV and image manipulation
jsonschema==4.23.0         # Optional: schema validation for JSON exports

# Optional/ Dev only
pyqt6-tools~=6.4           # Qt Designer